import java.lang.reflect.Array;
import java.util.ArrayList;
import java.util.List;

public class ParkingLot_Management {
    /**
     *
     */
    public Student getHighestPayer(List<Student> payments){
        Student max_payer = new Student();
        double max_payment = -1;

        //throw ticketRecord
        for(int i=0; i<payments.size();i++){

        }
        return max_payer;
    }
}
